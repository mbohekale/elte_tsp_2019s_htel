#!/bin/bash
# This a simple script project for hello world
# How to get variables and using them
# code for building and running a shell script
# On the command line code
# nano fileName
# On the editor
# First to name the file(#!/bin/bash)
# write the code and save(exit and yes)
# On the command line code
# building(chmod +x fileName.sh)
# running or excuting(./fileName.sh) 
echo "Hello world:$(date) @ $(hostname)"
